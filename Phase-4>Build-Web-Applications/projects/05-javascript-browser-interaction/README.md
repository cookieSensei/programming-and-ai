# Project 5 — JavaScript Browser Interaction

This project is intentionally independent of Django. Open `index.html` in a browser.

The goal is to become comfortable with variables, functions, events, the DOM, `console.log()`, and browser developer tools.

There is no build system, npm project, React, or framework.
