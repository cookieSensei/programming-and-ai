This project does not require a training dataset for its baseline.

For classroom testing, create a small set of known resume/job examples and
run evaluate_pipeline.py.

The application itself accepts a real resume PDF/image and a pasted job
description.
